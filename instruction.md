# Gemini Agent Instructions

## Project Overview

This is an e-commerce project with a `frontend` and a `backend`.

- The `frontend` is likely a web application.
- The `backend` provides the APIs for the frontend.

When working on the project, please follow these guidelines.

## Frontend

- When I ask you to work on the frontend, assume that the code is in the `frontend` directory.
- Be mindful of the frameworks and libraries being used.
- If you need to add new dependencies, please ask first.

## Backend

- When I ask you to work on the backend, assume that the code is in the `backend` directory.
- Pay attention to the database schema and API endpoints.
- When adding new endpoints, please document them.

## General Instructions

- Keep your responses concise and to the point.
- Use modern development practices.
- Maintain consistency with the existing code style.
- If you are unsure about something, please ask for clarification.
